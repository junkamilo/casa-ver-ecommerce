// Re-export del hook movido a `modules/collections/presentation/hooks/`.
// Se mantiene esta ruta para no romper consumidores existentes.
export { useCollection } from "@/modules/collections/presentation/hooks/use-collection";
